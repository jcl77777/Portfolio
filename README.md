# Certo Landing Page

Certo 訂閱管理 App 的官方登陸頁面，使用 Next.js 和 Tailwind CSS 構建。

## 🚀 快速開始

### 安裝依賴

```bash
npm install
# 或
yarn install
# 或
pnpm install
```

### 開發模式

```bash
npm run dev
# 或
yarn dev
# 或
pnpm dev
```

在瀏覽器中打開 [http://localhost:3000](http://localhost:3000) 查看結果。

### 建置生產版本

```bash
npm run build
# 或
yarn build
# 或
pnpm build
```

建置完成後，靜態文件將生成在 `out/` 目錄中。

## 📁 專案結構

```
landing-page/
├── components/          # React 組件
│   ├── Logo/          # Logo 組件
│   ├── Hero.tsx       # 首屏區塊
│   ├── Features.tsx   # 功能展示
│   ├── HowItWorks.tsx # 使用流程
│   ├── FAQ.tsx        # 常見問題
│   ├── Footer.tsx     # 頁尾
│   └── LanguageSwitcher.tsx  # 語言切換
├── contexts/          # React Context
│   └── LanguageContext.tsx   # 語言上下文
├── lib/               # 工具庫
│   └── i18n/         # 國際化
│       └── translations.ts  # 翻譯文件
├── pages/             # Next.js 頁面
│   ├── _app.tsx       # App 入口
│   ├── _document.tsx  # Document 配置
│   └── index.tsx      # 主頁
├── styles/            # 樣式文件
│   └── globals.css    # 全局樣式
├── public/            # 靜態資源
└── next.config.js     # Next.js 配置
```

## 🌍 多語言支援

網站支援繁體中文和英文，並提供語言切換功能：

- **自動檢測**：根據瀏覽器語言自動選擇
- **語言切換**：右上角固定按鈕
- **持久化**：語言偏好儲存在 localStorage
- **SEO 優化**：動態 Meta 標籤

詳細說明請看 `I18N_IMPLEMENTATION.md`

## 🎨 品牌規範

### 色彩

- **紫色漸層**：`#a855f7` → `#7c3aed`
- **黑色**：`#000000`
- **白色**：`#ffffff`
- **深灰**：`#1F2937`
- **淺灰**：`#F3F4F6`

### 字體

- **標題**：System Bold Rounded（或 Nunito/Poppins Bold）
- **內文**：System Regular
- **Letter Spacing**：`-0.5`

## 🚀 部署

### Vercel（推薦）

1. 將專案推送到 GitHub
2. 在 [Vercel](https://vercel.com) 導入專案
3. 自動部署完成！

### 靜態導出

專案已配置為靜態導出（`output: 'export'`），可以部署到任何靜態託管服務：

- **Netlify**：拖放 `out/` 目錄
- **GitHub Pages**：使用 GitHub Actions
- **AWS S3**：上傳 `out/` 目錄內容

## 📝 環境變數

目前無需環境變數。如需添加，創建 `.env.local` 文件。

## 🔧 技術棧

- **Next.js 14**：React 框架
- **TypeScript**：類型安全
- **Tailwind CSS**：樣式框架
- **React**：UI 庫

## 📄 授權

© 2025 Certo. All rights reserved.

